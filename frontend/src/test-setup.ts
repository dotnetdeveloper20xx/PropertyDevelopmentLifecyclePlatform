/**
 * Global test setup for Vitest.
 * Note: Zone.js is NOT imported here as it conflicts with Vitest.
 * Pure function tests (reducers, selectors, utils) work without Angular TestBed.
 * Service/component tests that need Angular DI should use alternative approaches.
 */
